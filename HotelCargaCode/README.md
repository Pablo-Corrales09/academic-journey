# HotelCarga

An ASP.NET Core MVC project for HotelCarga.

## Getting Started

To run the project:

1. Ensure you have .NET 10.0 installed.
2. Run `dotnet run` in the project directory.
3. Open https://localhost:5001 in your browser.